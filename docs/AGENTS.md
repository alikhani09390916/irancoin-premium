# AGENTS.md — IRANCOIN PREMIUM v2 (هسته هوش مصنوعی)

## واقعیت پروژه
- **پروژه:** IRANCOIN PREMIUM — ۳ صفحه کامل (لندینگ + تعرفه + داشبورد) با هسته هوش مصنوعی
- **وابستگی:** **هیچ** — بدون CDN، بدون Three.js، بدون npm، فقط Canvas/SVG/CSS خام

## ساختار فایل‌ها
```
├── index.html              صفحه اصلی (لندینگ + هیرو)
├── pricing.html             خرید اشتراک
├── dashboard.html           داشبورد
├── smoke-test.js            تست خودکار Node — بدون مرورگر
├── AGENTS.md                این فایل
├── assets/
│   ├── css/
│   │   ├── theme.css         سیستم طراحی مشترک (رنگ، تایپ، ناوبری، فوتر، دکمه)
│   │   └── ai-core.css       استایل هیرو: هسته، کارت‌ها، کابل‌ها، سکه‌ها
│   └── js/
│       ├── ai-core.js        کره چرخان + ۳ حلقه مداری (بدون وابستگی)
│       ├── live-candles.js   کندل‌های زنده پس‌زمینه (بدون وابستگی)
│       ├── cables.js         لایه SVG کابل‌های داده (بدون وابستگی)
│       └── ui.js             شمارنده‌ها، آکاردئون FAQ، تب‌ها، منوی موبایل
```

## دستورات
```bash
node smoke-test.js          # تست خودکار (باید PASSED شود)
# پیش‌نمایش: index.html را در مرورگر باز کنید
```

## قوانین
1. **فقط ادغام** — کدها تمام و تست شده‌اند؛ بازنویسی/بهبود ممنوع
2. **بدون وابستگی** — هیچ CDN یا کتابخانه بیرونی اضافه نکنید
3. **لوگو متنی** — "IranCoin" باید متن باقی بماند (نه تصویر)
4. **RTL-first** — `lang="fa" dir="rtl"` در HTML نهایی
5. **prefers-reduced-motion** — همه انیمیشن‌ها سازگار هستند
6. **تست قبل از تحویل** — `node smoke-test.js` باید PASSED شود
