# AGENTS.md — IRANCOIN PREMIUM (هسته هوش مصنوعی)

## واقعیت پروژه
- **پروژه:** IRANCOIN PREMIUM — بخش هسته هوش مصنوعی (AI Core) برای سایت اصلی
- **فایل‌ها:** ۴ فایل اصلی + ۱ تست + ۱ پیش‌نمایش + ۱ راهنمای ادغام
- **وابستگی:** **هیچ** — بدون CDN، بدون Three.js، بدون npm، فقط Canvas خالص

## فایل‌ها
| فایل | توضیح |
|------|-------|
| `ai-core.js` | کلاس اصلی — کره ذره‌ای/شبکه عصبی در حال چرخش + حلقه‌های مداری + گوی مرکزی |
| `ai-core-chart.js` | نمودار کندل‌استیک زنده (OHLC واقعی) با پس‌زمینه متحرک |
| `ai-core-connectors.js` | لوله‌های ارتباطی نورانی از مرکز (لوگو) به هر کارت/سکه |
| `ai-core.css` | استایل همه المان‌ها: کره، لوگو، لوله‌ها، کارت‌ها، سکه‌ها، پنل‌های نمودار |
| `preview.html` | پیش‌نمایش کامل با ۶ کارت، لوگو، لوله‌ها، ۲ نمودار زنده |
| `smoke-test.js` | تست خودکار (Node.js) — هندسه، محاسبات، OHLC، resize |
| `INTEGRATION_PROMPT.md` | پرامپت دقیق برای ادغام در سایت اصلی |

## دستورات
```bash
node smoke-test.js          # تست خودکار (باید PASSED شود)
# پیش‌نمایش: preview.html را در مرورگر باز کنید (کنار ۴ فایل دیگر)
```

## قوانین
1. **فقط ادغام** — کدها تمام و تست شده‌اند؛ بازنویسی/بهبود ممنوع
2. **بدون وابستگی** — هیچ CDN یا کتابخانه بیرونی اضافه نکنید
3. **حفظ ساختار** — ترتیب اسکریپت‌ها: `ai-core.js` → `ai-core-chart.js` → `ai-core-connectors.js`
4. **لوگو متنی** — "IranCoin" باید متن باقی بماند (نه تصویر)
5. **RTL-first** — `lang="fa" dir="rtl"` در HTML نهایی
6. ** prefers-reduced-motion** — همه انیمیشن‌ها سازگار هستند
7. **تست قبل از تحویل** — `node smoke-test.js` باید PASSED شود
